{{> _header }}
{{> _navbar }}
<div class="page-header">
    <h1>{{ account.name }}<small>{{ account.desc }}</small></h1>
</div>

    <h3>Email</h3>
    {{ account.email }}

    <h3>Name</h3>
    {{ account.name }}

    <h3>Namespace</h3>
    {{ account.namespace }}

<a class='btn btn-default btn-sm' href='{{ account.uri }}/edit'>
    Edit
</a>
{{> _footer }}
